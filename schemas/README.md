# Schemas

These schema files are first-pass contracts. They are intentionally small so we
can refine them after you provide the real JVTO data.

Recommended use:

1. Add raw data into the right core.
2. Validate each record shape against the matching schema.
3. Promote stable data into source-controlled files.
4. Keep dynamic customer and operations data in a database later.

